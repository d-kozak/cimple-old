

fn fib(int i) : int{
    if(i < 2){
        return 1;
    } else {
        return fib(i - 1)  + fib(i - 2);
    }
}

int i = 10;

for(int j = 0; j < i ; j = j+1){
    int res = fib(j);
    print res;
}